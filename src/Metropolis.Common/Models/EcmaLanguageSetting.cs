namespace Metropolis.Common.Models
{
    /// <summary>
    /// Some parsers (eslint) require different dialects of languages like Javascript require this specification
    /// </summary>
    public enum EcmaLanguageSetting
    {
        DEFAULT, ECMA3, ECMA5, ECMA6
    }
}